"""Broker plugin directory.

Drop a new ``my_broker.py`` file in here that exposes a ``register(registry)``
function and Trade-Claw will pick it up automatically on the next start.

See ``docs/CUSTOM_BROKERS.md`` for the step-by-step recipe and a skeleton
template.
"""
