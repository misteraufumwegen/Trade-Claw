# Trade-Claw Requirements
# Python 3.11+ (tested with 3.11–3.14; ranges chosen so wheels exist on each)

# FastAPI & Web
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.10,<3.0

# HTTP & Async
aiohttp>=3.10,<4.0
httpx>=0.27,<1.0
requests>=2.32,<3.0

# Database (SQLite built-in, PostgreSQL optional)
sqlalchemy>=2.0.30,<3.0
alembic>=1.13,<2.0

# Redis (optional - session management in production)
redis>=5.0,<6.0

# Cryptography & Security
cryptography>=43.0,<46.0

# Configuration
python-dotenv>=1.0,<2.0
pyyaml>=6.0,<7.0

# Numerical / ML
numpy>=2.0,<3.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<2.0
pytest-cov>=5.0,<7.0
