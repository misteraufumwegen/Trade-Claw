# Trade-Claw Phase 4 Requirements (extends requirements.txt)
# Install: pip install -r requirements.txt -r requirements_phase4.txt

# Broker-specific (install only if using these brokers)
ccxt==4.0.96              # Alpaca, Kraken, etc.
eth-account==0.10.0       # Hyperliquid (Ethereum signing)
web3==6.11.1              # Hyperliquid (Web3 interactions)
websockets==12.0          # Real-time price streaming

# PostgreSQL (production database)
psycopg[binary]==3.3.3

# Extended Security
PyJWT==2.8.1

# ML Pipeline (for neural network training)
torch>=2.0.1

# Monitoring & Logging
python-json-logger==2.0.7

# Development & Code Quality
ruff>=0.1.0
mypy==1.7.1
